- defaults:
    link:
      create: true
      relink: true
      force: true

- clean:
    ~/:
    ~/.config:

- link:
    ~/.zshrc:
      path: zsh/.zshrc
    ~/.config/nvim:
      path: nvim
    ~/.gitconfig:
      path: git/.gitconfig
    ~/.gitignore:
      path: git/.gitignore
